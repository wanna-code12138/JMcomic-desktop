# JMComic Desktop 1.0.4 开发进度

> 更新日期：2026-08-03

## 一、版本目标

1.0.4 的目标是把占位符"下载页"做成完整的下载管理，并打通**本地离线阅读**：

- 记录下了什么漫画，能从下载页快速点进去，且**从本地访问、不再重新加载网页**
- 在设置页提供下载功能专有设置（路径等）

## 二、已完成（均已合入 main）

### 1. 下载能力

- 详情页"下载"按钮：多章时弹章节选择器（默认全选，支持全选/全不选/反选/自选），单章直接下载；章节行有单章下载按钮；阅读器工具栏可下载当前章
- 主进程批量入队 `download:addChapters`：逐章抓图并入队，进度通过事件推送到顶部栏，不阻塞界面
- 队列并发 1–8、单图失败重试 0–6、取消 / 重试 / 删除（可连同本地文件）/ 打开文件夹
- 断点续传：已存在的页码自动跳过；重启后未完成任务恢复（见"遇到的问题"第 1 条）
- `downloads` 表新增 `chapter_url` / `cover_url` / `error` 列并带旧库迁移

### 2. 下载页

- "已下载"Tab：按漫画聚合卡片（封面、已下载 x/y 章、下载中/失败数），只展示有完成章节的漫画；点击进入本地详情 → 本地阅读器
- "任务"Tab：全部任务列表，含进度条、状态、失败原因、取消/重试/打开文件夹/删除

### 3. 本地离线阅读

- 新增 `jmlocal://` 协议直读下载目录（带路径白名单校验：仅允许下载根目录下的 漫画/章节/图片 三层结构）
- 本地详情视图：标题、封面、已下载章节、失败章节一键重试、打开文件夹
- 阅读器本地模式：直接扫描本地图片，不触发网页抓取、不重复反打乱

### 4. 设置页"下载"区块

- 下载目录（默认 `系统下载/JMComic`，可浏览选择）
- 同时下载章节数（1–8）
- 单张图片失败重试次数（0–6）
- 启动时自动续传（默认开）

### 5. 交互改进

- 章节选择弹窗确认后**立即关闭**，抓图/入队全部在后台
- 顶部栏新增下载指示："添加章节 x/y" → "下载中 n"，点击直达下载页

## 三、遇到的问题与修复

1. **启动自动续传在 app.ready 前执行 → 恢复的任务全部失败**

   下载管理器在模块加载时恢复 pending 任务，续传需要重新抓取图片列表，会在 app ready 前创建 BrowserWindow 直接抛错，导致重启后所有任务变 failed（用户实测：9 条任务全部失败、0 页，但图片其实已进全局缓存）。

   修复：`initDownloadManager()` 延后到 app ready 且会话暖机完成后调用。

2. **章节标题脏数据**

   站点章节链接文本被整段抓取，混入"開始閱讀"按钮文字、换行空白、`[2]` 等垃圾后缀，导致章节目录名怪异（例如 `漫画名/開始閱讀/`）。

   修复：标题清洗（过滤按钮词、压缩空白、去尾部 `[n]`）、缺失时回退"第 N 話"；目录名再压缩一次空白，保证每章独立目录。

3. **多任务并发反打乱互相干扰**

   多个下载任务并发调用单例隐藏 BrowserWindow 的 `executeJavaScript`，可能互相覆盖 `img` 元素导致结果错乱。

   修复：`imageDescrambler` 用 Promise 链串行化所有反打乱请求。

4. **取消失效（旧逻辑）**

   原实现取消后把任务置回 pending，队列会立刻把它重新捡起。

   修复：取消后置为 `cancelled`，重试需用户显式点击。

5. **旧任务缺少 chapter_url / cover_url**

   旧版本不记录章节 URL 和封面，无法续传、本地详情无封面。

   修复：加列迁移；详情页/阅读器入队时补录。

6. **失败原因无处可查**

   新增 `error` 列，任务失败原因入库并在任务列表/本地详情展示。

7. **UI 细节**

   - 安装的 Fluent UI 版本没有 `LinearProgress` → 自绘进度条
   - 章节弹窗 effect 依赖数组导致父组件重渲染时把用户选择重置为全选 → 改为仅在打开时初始化

8. **开发环境坑**

   - `tsx` 在受限沙箱内报 `uv_os_get_passwd ENOMEM`，测试需提权运行
   - PowerShell 对含 `[ ]` 的路径会按通配符处理，排查目录时曾误判"文件丢失"（需 `-LiteralPath` / `-Force`）
   - 仓库存在一批历史 tsc 类型错误（contentApi / ipc / siteAdapter / sessionWarmup / MangaCard / auroraTheme 等），与本次任务无关，未处理

9. **详情页大概率显示 `jmcomic.me` 占位符（进行中，尚未修复）**

   现象：从主页点进详情页，大概率标题显示 `jmcomic.me`、内容无法正常加载；其他功能也可能连带不可用（2026-08-03 反馈）。

   根因分析（代码审计结论，本机沙箱无法直连站点复现）：
   - `networkProbe` 轮询时只要求 `HEAD /` 状态码 < 500 就判可用（`networkProbe.ts:117`），`jmcomic.me` 返回的占位页是 200，会被误选为 `activeDomain`（硬编码候选 `DEFAULT_DOMAINS` 含 `jmcomic.me`）；之后所有抓取都导航到占位页，标题就提取成 `jmcomic.me`
   - 官方 JM 库（hect0x7/JMComic-Crawler-Python）说明网页域名动态变化：`jm365.work/3YeBdF` 永久跳转 + `jmcomicgo.org` 发布页动态获取；`jmcomic.me` 不在官方域名语义内
   - `scraperWindow.navigateAndWait` 超时或 `ERR_ABORTED` 后仍直接继续提取，可能对旧页面/占位页执行 DOM 提取
   - `extractMangaDetail` 提取后无条件 `cacheSet`（TTL 10 分钟），坏结果会滞留并反复命中

   已做：
   - 新增 `src/main/__tests__/networkCore.test.ts`（TDD 红阶段，尚未运行通过）：规划纯逻辑模块 `networkCore.ts`，导出域名规范化、发布页域名提取、JM 页面真伪校验、路径匹配四个函数
   - 已调研官方 JM 库的域名机制与解析正则（`jm_config.py` / `jm_toolkit.py`）

   还没做：
   - `networkCore.ts` 尚未实现，测试未跑通
   - `networkProbe` 接入"真页面校验 + 动态域名表 + 探测失败不改 activeDomain"未完成
   - `scraperWindow` 导航目标校验/重试、提取前页面真伪校验、坏结果不缓存未完成
   - 未建分支、未提交、未合入 main

## 四、尚未完成 / 待办

- **详情页占位符 bug（进行中）**：见"三、遇到的问题与修复"第 9 条，修复方案已定、尚未实施
- **封面仍是网络代理加载**：下载记录里的封面走 `jmimg://`，离线时封面可能不显示（本地未保存封面）
- **旧目录不自动迁移**：已产生的脏目录（如 `開始閱讀`）不会自动改名/搬移，需手动删除后重新下载
- **打包未验证**：还没跑 `npm run package` 产出 1.0.4 便携 exe
- **本地阅读器无章节切换**：读完一章要返回本地详情再点下一章
- **历史续读仍是网页模式**：收藏页"历史记录"的续读不会自动走本地文件
- **反打乱串行化是单线程瓶颈**：多章下载时图片处理较慢，后续可做窗口池或并发改造
- **下载清理不彻底**：删除整本时只删章节目录，可能残留空漫画目录
- **工作区有未提交的 `docs/superpowers` 删除**（非本任务改动，保持原状未动）

## 五、验证与提交

### 验证结果（真实输出）

- 测试：7 个测试套件全部通过（homepageLogic / homepageStream / dataPaths / imageCacheCore / personalData / settingsCore / downloadCore）
- 构建：`npm run build` 通过（版本 1.0.4）
- 类型：本次改动文件 `tsc --noEmit` 零错误

### 提交记录（均在 main）

- `c7a26a5` feat(1.0.4): 实现下载页与本地离线阅读
- `66feaa6` fix(1.0.4): 修复下载失败与目录结构，下载进度移至顶部栏
- `42ef4a2` docs: 新增 PROGRESS.md 记录 1.0.4 进度、遗留问题与踩坑
- 分支 `feat/1.0.4-downloads`、`fix/1.0.4-download-issues` 已合并并删除

### 进行中的改动（未提交）

- `src/main/__tests__/networkCore.test.ts`：新增的详情页占位符修复测试，尚未实现对应模块
- 工作区另有未提交的 `README.md`、`docs/superpowers/*` 删除（非本任务改动，保持原状未动）

## 六、自测步骤

1. `npm run dev`
2. 打开漫画详情 → "下载" → 选章确认（弹窗立即关闭，顶部栏显示"添加章节 x/y"）
3. 下载页"任务"Tab 看进度/失败原因；"已下载"Tab 点击漫画 → 本地详情 → 离线阅读
4. 设置页"下载"区块改目录/并发/重试/续传
5. 重启应用验证自动续传；对失败任务点"重试"
